# OpenCode tool fixture

Captured from OpenCode 1.18.29 using `opencode/mimo-v2.5-free` in the real
native chat conformance harness. The disposable task located and read one
scratch file; its secret was absent from the prompt. These are native SQLite
tool parts joined to their assistant messages, not reconstructed terminal text.

Only tool identity, structured input, terminal output, timestamps, and parent
identity are retained. The scratch workspace path is replaced with a placeholder;
provider display titles and unrelated message metadata are omitted. The probe
content and identifiers belong solely to this disposable test.
